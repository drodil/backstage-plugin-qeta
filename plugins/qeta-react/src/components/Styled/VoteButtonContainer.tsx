import { styled } from '@mui/system';

export const VoteButtonContainer = styled('div')({
  textAlign: 'center',
  width: '32px',
  marginLeft: '5px',
  display: 'inline-block',
  verticalAlign: 'top',
});
